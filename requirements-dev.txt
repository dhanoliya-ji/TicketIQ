# ---------------------------------------------------------------------------
# Development / CI dependencies. Install on top of requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt
# ---------------------------------------------------------------------------
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0          # required by fastapi.testclient
# The formatter and linters are pinned exactly, not floated. A new black
# release reformats the whole codebase, and these same versions are named in
# .pre-commit-config.yaml - if the two drift apart, pre-commit rewrites a file
# and CI then rejects its own output, forever. Bump both together.
black==26.5.1
ruff==0.16.7
mypy==2.3.1
pre-commit>=3.6.0
